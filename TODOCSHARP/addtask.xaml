<Window x:Class="TODOCSHARP.addtask"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TODOCSHARP"
        mc:Ignorable="d"
        Title="addtask" Height="450" Width="800" ResizeMode="NoResize">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="0.5*"></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Text="Taper la tâche à ajouter" FontSize="40" VerticalAlignment="Center" HorizontalAlignment="Center"/>
        <TextBox Grid.Row="1" x:Name="task" Margin="50,50,50,50" />
        <TextBox Grid.Row="2" x:Name="description" Margin="50,0,50,0" />
        <Button x:Name="btn_add" Content="Ajouter" HorizontalAlignment="Right" VerticalAlignment="Top" Grid.Row="3" Height="50" Width="100" Margin="0,10,50,0" Click="btn_add_Click"/>
    </Grid>
</Window>
