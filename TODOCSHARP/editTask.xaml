<Window x:Class="TODOCSHARP.editTask"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TODOCSHARP"
        mc:Ignorable="d"
        Title="editTask" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.2*"/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <TextBlock Text="Modifier la tâche" Grid.Row="0" FontSize="30" VerticalAlignment="Center"  HorizontalAlignment="Center"/>

        <TextBox Grid.Row="1" x:Name="task" Margin="50,50,50,50" />
        <TextBox Grid.Row="2" x:Name="description" Margin="50,0,50,0" />
        <Button x:Name="btn_edit" Content="Ajouter" HorizontalAlignment="Right" VerticalAlignment="Top" Grid.Row="3" Height="50" Width="100" Margin="0,10,50,0" Click="btn_edit_Click"/>


    </Grid>
</Window>
